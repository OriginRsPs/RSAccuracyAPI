package com.origin.accuracy.api;

/**
 * RSNpc interface represents a non-player character (NPC) in the game.
 */
public interface RSNpc {

    /**
     * Retrieves the NPC itself.
     *
     * @return The NPC itself.
     */
    RSNpc get();
}
